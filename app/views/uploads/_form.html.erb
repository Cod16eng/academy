<%= form_with(model: upload, local: true) do |form| %>
  <% if upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload.errors.count, "error") %> prohibited this upload from being saved:</h2>

      <ul>
      <% upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :upload %>
    <%= form.text_field :upload %>
  </div>

  <div class="field">
    <%= form.label :webinar_id %>
    <%= form.number_field :webinar_id %>
  </div>

  <div class="field">
    <%= form.label :speaker_id %>
    <%= form.number_field :speaker_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
